# Balder (@Balder13946731) 交易体系深度分析

> 素材：2026-07-31 AMZN/MSFT Earnings Radar 两张图 + 07-31 解释帖 + 08-12「泄露商业机密」帖 + 09-08「本来想删掉」帖 + 8-9 月时间线（短线/长线机器人、复盘、WMT RSI 帖等）。

---

## 0. 一句话结论

Balder 的系统 **不是** 让 LLM（他叫 "Fable"）去预测财报好坏；它是一个 **「结构化 schema 驱动的期权资金流读取器」**，LLM 只是在一套固定的量化骨架上做「有边界的裁判」，而真正的 alpha 来源于：

1. 把期权市场里 **已经用真金白银下注的信息**（P/C、skew、新增头寸 v/OI）当作主信号；
2. 用 **企业基本面惯性**（beat rate、历史反应）给方向定调；
3. 用 **replay + 自进化提示词** 把历史校准成可复用的规则，而不是每次靠模型「灵感」。

---

## 1. 从两张图逆向出的数学骨架

我用两张图上公开的全部数字做了反推，**所有核心公式都能同时命中 AMZN 与 MSFT 两组数据（误差 ≤ 0.01）**，见 `tests/test_anchors.py`。

| 面板项 | AMZN | MSFT | 反推公式 | 复现值 |
|---|---|---|---|---|
| expected move | ±6.7% | ±6.1% | `0.6·implied + 0.4·hist_median` | 6.74 / 6.14 |
| system lean (τ-adj) | +0.39 | +0.34 | `0.5·core + 0.15·flow_z + 0.2·rs_phase` | 0.391 / 0.346 |
| 分桶边界 | ±3/±7/±13 | ±3/±6/±12 | `±3, ±round(EM), ±round(2·EM)` | ✓ / ✓ |
| model tilt (μ/EM) | +0.128 | −0.243 | `0.23·(−skew/10) + 0.10·(0.70−P/C) + 0.10·(fresh−1) + 0.10·lean` | 0.129 / −0.243 |
| 分布 | 7 桶 | 7 桶 | Student-t，df≈3.4 (AMZN) / 4.5 (MSFT)，scale≈EM | 桶误差 ≤3pp |
| P(up) | 55% | 42% | `1 − T_cdf(0; μ, scale, df)` | 0.550 / 0.410 |

几个值得注意的**设计信号**：

* **"τ-adj"**：所有 positioning 信号按距财报天数做半衰期收缩——离事件越远，读到的 positioning 越不可信。这说明他每天/每 5 分钟刷新，而不是财报前一天才看。
* **分布用 t 而不是正态**：AMZN 新增资金 v/OI 1.25（疯狂）→ df 更低、尾更肥；MSFT 0.5 → df 更高。**flow 强度决定尾部**，不只是决定方向。
* **P(up)=42% 但 OUR CALL 仍是 BULLISH LEAN（MSFT）**：这是最重要的一条线索——**方向由 system lean 决定，P(up) 只负责 LEAN→CONVICTION 的升级**。也就是：基本面惯性 + 相对强弱定调子，期权分布只定置信度。这和他 07-31 帖里「模型顺应了这股力量」的说法一致。

## 2. 三层信号的分工

```
                ┌──────────────────────┐
  期权微观结构   │ IV skew (put−call)   │ → 谁在为哪边保险付钱
  ("聪明钱底牌") │ P/C volume           │ → 今天资金押哪边
                │ fresh positioning v/OI│ → 有多少是"新钱"
                └──────────┬───────────┘
                           ▼  tilt（分布位移）+ 尾部肥瘦
  企业惯性       ┌──────────────────────┐
  ("core")      │ beat rate last 8      │ → 8/8 beat = +0.8 core
                │ up-rate / drift       │
                │ 共识离散度收缩         │
                └──────────┬───────────┘
                           ▼  system lean（方向）
  盘面          ┌──────────────────────┐
                │ flow z(5d)            │ 美元成交量异常
                │ rs_phase              │ 相对 SPY 强弱的离散档位(±0.3/±0.6/±1)
                └──────────────────────┘
```

**为什么这样分工是对的**（他帖子里的市场逻辑）：

* 财报是「已知的未知」——**波动的量**是可以从期权价格里读出来的（implied vs hist 面板就是在回答"期权贵不贵"）；
* **波动的方向**不能靠猜 EPS——但能看谁在付溢价：put skew 高 = 机构在买保护 = 多头害怕；P/C 0.46 = 看涨成交量是看跌的 2 倍 = 有人在赌上；
* **fresh v/OI > 1** = 今天成交量超过整个存量持仓 = 有大量"新钱"紧急进场，而不是老仓位在挪；
* 这些都不依赖任何"预知"，而是**市场潜台词的量化**。

## 3. "Fable 怎么预测财报"：schema 种子 + 提示词元学习

08-12 帖的原话拆解 → 对应实现：

| 他的描述 | 含义 | 本仓库对应 |
|---|---|---|
| 「没有结构性的输入就不会有结构性的输出」 | 不许直接问 LLM "怎么看财报" | `fable/llm.py` 只接受 `EarningsSchema` JSON，输出必须是 JSON |
| 「一个 schema 作为种子，包含期权、订单、历史等」 | 统一数据契约 | `schema.py`：`OptionsBlock / HistoryBlock / StreetBlock / TapeBlock` |
| 「以这个 schema 作为种子重建过去财报的相同输入」 | 点时（point-in-time）回放 | `data/provider.build_point_in_time()`；只用 T−1 收盘前的数据 |
| 「配置自我进化的机制，进行提示词的元学习」 | 系统提示 = 规则库；规则由 LLM 从 replay 表里提出 | `fable/memory.RuleBook`（规则即提示词）、`Fable.propose_rules()` |
| 「监控提示词，观察他有没有强行记住不合理的记忆来约束过拟合」 | 静态 + 动态过拟合监控 | `validate_rule()`（禁止 ticker/日期/过精数字，≤3 条件，\|adj\|≤0.08）+ `_passes_monitor()`（in/oos 支持度、oos 命中率、符号翻转→隔离） |
| 「疯狂的 replay 和 training」 | 走前（walk-forward）多轮进化 | `replay/engine.evolve()`，70/30 时间切分 |

**这套设计的精髓**是：LLM 的"学习成果"被强制物化为**可机器验证的 if-then 规则**（`options.pc_volume < 0.6 AND fresh > 1.0 → p_up += 0.05`），而不是留在对话上下文里。这样：

* 规则可以在样本外被打分、被淘汰；
* 规则不会夹带"AMZN 总是涨"这种记忆；
* 推理时的提示词就是当前活跃规则 → 提示词真正在"进化"。

我实际跑了一轮（14 只股票 × 10 次财报 = 140 行），gpt-5-mini 提了 6 条规则，**过拟合监控隔离了 5 条**（样本内 51% → 样本外 33% 这种），只留下 1 条 `ret_20d ≥ 10 & RSI 高 → 减 3pp`（oos 58%）。这就是他说的「约束过拟合」在真实数据上的样子——**大部分"看起来有道理"的规则活不过样本外**。

## 4. 从「一个季度一次」到「每 5 分钟一次」

09-08 帖：「原理和这个帖子差不多，就是写一个有一定结构的 skill，配合自进化以及每 5 分钟询问一次」。

结合时间线上的机器人行为，可以还原出两个 bot：

**短线机器人**（NFLX +1.27% 平仓、RGTI −3.97% 止损、ASTS/AAPL/CSCO/APP/UNH/QBTS/IONQ 持仓）
* 因子：VWAP 上方、30 分钟成交量倍数、30 分钟动量、相对强弱、5 分钟 RSI 过热惩罚、开盘 15 分钟噪声惩罚、**财报当晚禁止裸多**
* 止损紧（−2.5% 或跌破 VWAP）；胜率不高（1/2、−3.97%）但单笔亏损受控。

**长线机器人**（「只会抄底，最近抄了 ORCL 和 NVDA」、「WMT 大跌 10%，RSI 低于 39%，可以进行观察了」）
* 触发：日线 RSI < 40 **且** 20 日相对强弱未破位 **且** 距 52 周高点 < 25%；单日 −5%/跳空 −5% 加分；**财报前 2 天不抄**。
* 只做多、只抄底、RSI 回到 65 且盈利才走 → 非常低频、高筛选（「并不是每天都会有特别好的交易」）。

「两个机器人都会被一直维护其有效因子」= 同一套 `RuleBook` 进化机制套在 5 分钟 schema 上。

## 5. 他为什么"准"（以及边界）

**准的来源**（07-31 帖自述 + 数据验证）：
1. 只在**多信号共振**时给强调子（MSFT: beat 100%、lean +0.34、P/C 0.74，即便 skew +9.7 也只降 P(up) 不改方向）；
2. 用 **t 分布 + 7 桶**展示，而不是给点预测——大部分结果落在 ±3% 桶（35%）里本身就不会"错"；
3. 「不会作为直接交易信号，只作为模型对市场的基本认识」（08-03 投票帖）——他自己承认 Fable Picks 在交易上不准，**Earnings Radar 是 positioning 读数，不是买卖指令**。

**边界 / 我在复现中发现的坦白之处**：
* 我用 yfinance 免费数据的 replay：核心模型方向命中 42%、Brier 0.247 vs 基线 0.2475——**在没有真实历史期权链的情况下，量化骨架本身没有显著 edge**，接近随机。这符合预期：他的 alpha 主要在**实时期权 positioning**（P/C、skew、fresh），而历史期权链要付费（ORATS/CBOE DataShop）。
* 用**实时**期权链跑 ORCL（2026-09-10 财报）：skew −16.9pp（call 被大量买入）→ P(up) 65%，方向信号清晰得多。
* `implied_over_realized ≈ 1.07-1.08`：期权对财报的定价平均贵 7-8%，方差风险溢价存在但不大，说明「卖财报 IV」不是这套系统的主策略。

## 6. 复现要点（本仓库）

```
fable_agent/
  schema.py              ← 种子 schema（Balder 的核心）
  model/core.py          ← 逆向出的量化骨架（anchor 测试保证不漂移）
  data/provider.py       ← live / point-in-time 两种构建方式
  fable/memory.py        ← 规则库 = 可进化的提示词 + 过拟合守卫
  fable/llm.py           ← Fable：结构化 judge + propose_rules
  replay/engine.py       ← replay、评分（Brier/PIT/coverage）、walk-forward 进化
  intraday/loop.py       ← 5 分钟 schema + 短线/长线机器人 + Balder 风格 tape
  render/dashboard.py    ← 复刻 Earnings Radar 卡片
tests/                   ← 20 个测试：anchors、过拟合守卫、泄漏检查
```

要把它变成真正接近他的东西，缺的不是代码而是**数据**：历史期权链（做真正的 replay）、实时 L2/期权成交（做 fresh flow），以及几千次 replay 迭代的 token 预算——这正是他说「这个过程挺复杂并且消耗大量 token」的部分。
